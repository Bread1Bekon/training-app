services:
  db:
    image: postgres:13
    env_file: .env
    volumes:
      - pg-data:/var/lib/postgresql/data
    ports:
      - "6000:6000"

  app:
    build: .
    env_file: .env

    ports:
      - "8000:8000"
    depends_on:
      - db

volumes:
  pg-data: